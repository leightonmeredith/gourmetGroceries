using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace GG.Web.Models
{
    public partial class ModelFactory
    {
        //internal static ICollection<T> CreateObject(ICollection<T> models, IEnumerable<T> objs)            
        //{
        //    foreach (var obj in objs)
        //    {
        //        var model = Create(obj);
        //        models.Add(model);
        //    }
        //    return models;
        //}
    }
}
